# 作業時の案内

## 読む資料

最初に `CURRENT_STATE.md` と `docs/planning/IMPLEMENTATION_PROGRESS.md` を確認し、`docs/planning/IMPLEMENTATION_PLAN.md`（文書版2.0）の作業段階と必須検査を読みます。プレイヤー向け仕様は `docs/spec/CURRENT_GAME_SPEC.md` です。補助資料は必要な範囲だけ読みます。

最新仕様は常時発光・回転のみです。`docs/archive/`、旧データ例、`prototype/` は旧規則の履歴・比較資料で、最新仕様を上書きしません。画像中の古い問題数・環数・切替操作を実装しません。

## 変更の境界

R0（GitHub PR #5）は文書のみの段階としてマージ済みです。通常の実装依頼は計画の該当段階を実装・検査し、別観点のレビューと必要な修正を行い、Draft PRまで進めます。実装範囲と必須検査は進捗表と計画書の該当段階に従います。

各段階は直前のPRをユーザーがマージした最新mainから開始します。mainへの直接push、PRのマージ、自動マージ、強制的な履歴書換え、保護設定の緩和は禁止です。未マージの段階を土台に次を重ねません。

正式版は旧試作の一括コピーにせず、使える判定を検査付きで責務ごとに移します。ランキング、プレイ回数連携、Supabase、本番公開、実験場側の変更は今回のゲーム本体整備と分けます。公開は計画のR6とユーザーの公開指示を待ちます。

採用済みの規則を変更する必要がある場合は影響を示し、承認なく変更しません。計画の初期検証案は確定済み仕様ではありません。数値を変更するときは理由・版・再検査を残します。未決定の正式名、秘密値、外部サービス設定を推測しません。

## 検査と報告

計画の必須検査を省略しません。実装と独立した観点のレビューを分け、別担当でない場合は独立担当レビューと呼びません。実機に触れていない結果をiPhone実機確認済みとしません。

成功・失敗・未実施・対象外を分け、基準コミット、実行環境、検査結果、修正後結果、未確認、実機有無、次段階をPRと進捗表へ残します。必須項目が未完了なら一般公開可と報告しません。難度・検査・安全上の制限を緩めて成功扱いにしません。
