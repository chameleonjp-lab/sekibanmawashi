# 作業時の案内

## 読む順番

1. `CURRENT_STATE.md`
2. `docs/spec/CURRENT_GAME_SPEC.md`
3. `docs/planning/IMPLEMENTATION_PLAN.md`
4. 作業対象のデータ例または補助資料

`docs/archive/`は過去案です。現在の仕様判断には使いません。デザイン画像は見た目の参考に限り、画像をゲーム素材として貼り付けたり、画像中の古い問題数や環数を実装したりしません。

`prototype/`は確認用の試作です。公式の実装段階ではありません。公式作業は計画書のPR1から始めてください。試作のコードを `src/` へそのまま移さないでください。

## 変更の境界

- `main`へ直接pushしないでください。
- Pull Requestをマージしたり、自動マージを有効にしたりしないでください。
- 各実装段階は、直前のPull Requestが取り込まれた最新の`main`から始めてください。
- 採用済みの規則を変える必要がある場合は、実装前に文書の変更点と影響範囲を示してください。
- 正式名、秘密情報、外部サービスの未決定値を推測しないでください。

## 検査と報告

計画書にある必須検査を省かないでください。実装担当と独立した観点のレビューを分けます。自動ブラウザ検査をiPhone実機確認と呼ばず、実機で確認していない項目は未確認と報告してください。

失敗した検査がある状態で完成と報告しません。検査名、結果、確認できなかった点、次に必要な作業をPull Requestへ残してください。
